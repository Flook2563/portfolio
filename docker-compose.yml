version: '3.8'
services:
  portfolio:
    image: botflook/portfolio:latest
    container_name: portfolio-app
    ports:
      - "3100:80"  # ใช้ port 3100 สำหรับ internal communication
    restart: unless-stopped
    environment:
      - NODE_ENV=production